{% extends "@App/layout.html.twig" %}
{%  block title %} {{ title }} {% endblock %}
{% block content %}
    <table class="table table-condensed">
        <tr>
            <th>Título</th>
            <th>Fuente</th>
            <th>Publicado en</th>
            <th>Fecha</th>
            <th>Acciones</th>
        </tr>
    {% for feed in feeds %}
        <tr>
            <td>{{ feed.title }}</td>
            <td>{{ feed.source }}</td>
            <td>{{ feed.publisher }}</td>
            <td>{{ feed.created | date('d/m/Y H:i') }}</td>
            <td>
                <a class="btn btn-sm btn-outline-warning" href="{{ url('edit_feed', {'id': feed.id}) }}">Ver</a>
                <a class="btn btn-sm btn-outline-danger" href="{{ url('delete_feed', {'id': feed.id}) }}">Eliminar</a>
            </td>
        </tr>
    {% endfor %}
    </table>
{% endblock %}